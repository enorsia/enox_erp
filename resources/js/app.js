/**
 * Main Application Entry Point (Vite)
 *
 * CSS: All stylesheets are imported via app.css
 * JS: Template JS & libraries are loaded as regular <script> tags
 *     in the layout (they need browser global scope).
 */

// Import all CSS (processed by Vite)
import '../css/app.css';

