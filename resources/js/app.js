
// ── jQuery (global) ──
import $ from 'jquery';
window.$ = window.jQuery = $;

// ── Select2 ──
import 'select2';

// ── SweetAlert2 ──
import Swal from 'sweetalert2';
window.Swal = Swal;

// ── Common module ──
import './common';

